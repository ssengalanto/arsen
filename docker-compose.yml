services:
  api-dev:
    build:
      context: .
      target: dev
    profiles: [dev]
    ports:
      - "${SERVER_ADDRESS:-8080}:8080"
    volumes:
      - .:/app
      - go-modules:/go/pkg/mod
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  api-prod:
    build:
      context: .
      target: prod
    profiles: [prod]
    ports:
      - "${SERVER_ADDRESS:-8080}:8080"
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: arsen
      POSTGRES_PASSWORD: arsen
      POSTGRES_DB: arsen
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U arsen -d arsen"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  postgres-data:
  redis-data:
  go-modules:
